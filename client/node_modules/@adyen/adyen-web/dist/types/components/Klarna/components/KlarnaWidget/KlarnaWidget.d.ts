import { h } from 'preact';
import { KlarnaWidgetProps } from '../../types';
export declare function KlarnaWidget({ sdkData, paymentMethodType, payButton, ...props }: KlarnaWidgetProps): h.JSX.Element;
