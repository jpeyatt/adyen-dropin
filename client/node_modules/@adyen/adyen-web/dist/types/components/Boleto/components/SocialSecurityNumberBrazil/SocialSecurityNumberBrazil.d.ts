import { h } from 'preact';
export default function ({ onChange, onInput, valid, error, data }: {
    onChange: any;
    onInput: any;
    valid?: boolean;
    error?: any;
    data?: string;
}): h.JSX.Element;
