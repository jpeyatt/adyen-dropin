import Service from "../../../service";
import Resource from "../../resource";
declare class AmountUpdates extends Resource {
    constructor(service: Service, paymentPspReference: string);
}
export default AmountUpdates;
